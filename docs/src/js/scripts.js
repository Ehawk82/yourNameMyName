/*var init = () => {
	buildUI();
},
buildUI = () => {
	var container = createEle("div");

	container.innerHTML = "hello container";
	container.className = "container w3-container w3-black";

	body.append(container);
};

window.onload = () => {
	init();
};
*/